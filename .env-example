# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL=

# Application configuration

NODE_ENV=development # development | production
API_PORT=4000
API_URL=http://localhost:4000
APP_URL=http://localhost:3000
CORS_ALLOWED_URLS=http://localhost:3000,https://example.com

# Authentication configuration
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
SESSION_SECRET=
COOKIE_DOMAIN=

## Google
GOOGLE_CLIENT_ID=xxxx
GOOGLE_CLIENT_SECRET=xxxx

# Email configuration
## When enabled, all emails will be sent to this address instead of the actual recipients
## Useful for testing email functionality without spamming real users
DEVELOPMENT_EMAIL_SEND_TO=

# Email configuration
## Don't need to configure connection string if you are using specified SMTP configs or vice versa
SMTP_CONNECTION_STRING=

SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# STRIPE SETTINGS

STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_SUCCESS_URL=
STRIPE_CANCEL_URL=

## S3 FILE UPLOAD CONFIGURATION

# AWS S3 CONFIGURATION (Required)
# AWS Region where your S3 bucket is located
AWS_REGION=us-east-1
# AWS Credentials for S3 access
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# S3 Bucket name where files will be uploaded
AWS_S3_BUCKET=
# AWS CloudFront distribution URL for media files
AWS_CLOUDFRONT_MEDIA_URL=
# FILE UPLOAD VALIDATION
# Maximum file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760
# ALLOWED FILE TYPES
# Comma-separated list of allowed MIME types
# Default: image/jpeg,image/png,image/gif,image/webp,application/pdf,text/plain,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document