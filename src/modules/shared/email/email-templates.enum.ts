export enum EmailTemplate {
  WELCOME = 'welcome',
  PASSWORD_RESET = 'password-reset',
  NOTIFICATION = 'notification',
  SUBMIT_LEADS = 'submit-leads',
  TEST_TEMPLATE = 'test-template',
}

export default EmailTemplate;
